
->where('post', '[A-z_\-]+');//impose a condition on a slug present in the path

Caching:
cache('posts.all') //check value in cache for specified attribute
cache()->forget('posts.all');//empty cache for specified value

Debug loop in html ph file:
@dd($loop) in the loop

Blade components
option 1:
@extends('components.layout')

@section('content')  //USE THE SAME NAME AS THAT USED IN THE LAYOUT
   CODE
@endsection

option 2:

<x-layout>
   <x-slot name="content> //name of the block of code in the layout floder.by default its $slot
          CODE
   </x-slot>

</x-layout>

Forms & authentification
Eps45:
-To cretae new controller "php artisan make:controller ControllerName"
-ERROR 419 add CRSF
to check value of variable in controller use var_dump(request()->all()); or @dd

Eps46:
Laravel mutator exple:
public function  setPasswordAttribute($password)
    {
        $this->attributes['password'] = 'test';
    }
Laravel accesor exple
public function getUsernameAttribute($username)
    {
        return ucwords($username);  //whenever retiriving attribute username from db wil be capitalisaed.This change is done in User.php i.e the model
    }

Eps
